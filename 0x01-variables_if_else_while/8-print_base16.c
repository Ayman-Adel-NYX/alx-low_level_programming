#include <stdio.h>

/**
 * main - entry point
 *
 * Descraption: fkf
 *
 * Return: 0 (success)
 */

int main(void)
{
	char letter;
	int n;

	for (n = 0; num <= 9; n++)
		putchar((num % 10) + '0');
	for (letter = 'a'; letter <= f; letter++)
		putchar(letter);
		putchar('\n');

		return (0);
}
